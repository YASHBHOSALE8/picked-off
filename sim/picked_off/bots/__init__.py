"""Research bots (DESIGN.md §4). Sim-only in v1: the web app ships no bots.

Bots see only the censored public feed (own fills and own quotes) — never
V, trader types, declines, or the raw event stream.
"""
